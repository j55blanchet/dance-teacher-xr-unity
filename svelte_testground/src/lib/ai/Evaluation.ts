
/**
 * Compute the similarities between two 
 * @param refPose Reference Pose (row in CSV file)
 * @param mpResult User Pose (from mediapipe)
 * @returns Score indicating the similarity, between 0 and 1
 */
export function compareSkeletons2DVector(refPose: any, mpResult: any) {
    return 0;
}
